import styled from 'styled-components';

const Image = styled.img`
background-color: transparent;
`;

export const CustomFooterImage = styled(Image)`
    height: auto;
    max-width: 320px;
    padding-bottom: 25px;
    padding-top: 10px;
`;